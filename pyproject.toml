[project]
name = "secureagent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "langchain",
    "langgraph",
    "fastapi",
    "pydantic",
    "langfuse",
    "chromadb",
    "python-dotenv",
    "pytest>=9.0.3",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["src/tests"]
